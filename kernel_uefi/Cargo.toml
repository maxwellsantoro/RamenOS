[package]
name = "kernel_uefi"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "kernel_uefi"
path = "src/main.rs"
test = false
bench = false

[dependencies]
kernel = { path = "../kernel", features = ["test_protocols"] }
uefi = { version = "0.27", default-features = false }
